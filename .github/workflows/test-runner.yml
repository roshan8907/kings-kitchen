name: Test Self-Hosted Runner

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: self-hosted

    steps:
      - name: Check VM
        run: |
          echo "Running on:"
          hostname
          echo "Operating system:"
          uname -a
          echo "Docker version:"
          docker --version
